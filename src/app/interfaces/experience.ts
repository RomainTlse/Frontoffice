export interface Experience {
  date: string;
  titre: string;
  entreprise: string;
  client: string;
  preembauche: string;
  adresse: string;
  context: Array<string>;
  realisations: Array<string>;
  techologies: Array<string>;
  outils: Array<string>;
}
